# Žilet implementation and acceptance plan

Specification: ../../../zilet-astra-brief/PROMPT.md. User additionally requires Docker and `make up` / `make down`.

1. Identity and public slice. Compare two actual font specimens and two vector studies. Build masthead, homepage, poem and criticism from exact source fixtures. Verify rendered 390/768/1440 widths plus 320 overflow, then two recorded visual revisions before editorial expansion.
2. Durable model. PostgreSQL/Drizzle users separate from authors; versioned posts, ordered media, rubrics, placements, comments, moderation history and DB rate limits. Verify exact poetry round trip, concurrent revision conflict, publication promotion and private drafts.
3. Reader and public discovery. Better Auth verification/recovery, account UI, comments and moderation. Archive/search/author/about, safe HTML, metadata, image viewer and printing. Verify account ownership, role enforcement, recovery, back navigation and moderation freshness.
4. Editorial desk. Localised Tiptap prose plus exact-text poetry surface; autosave, private preview, publication, revisions, images, placements, comment moderation, honest Umami integration. Verify real publishing without rebuild, upload validation and unconfigured analytics state.
5. Delivery. Pinned containers, Makefile, environment example, persistent volumes, mail sink, owner tooling, migrations, backup/restore, identity notes, editor guide, acceptance and launch reports. Verify production build, automated tests, real browser flows, Compose startup/restart/down and restoration where possible.

The requested order is a non-skippable gate: inspect and refine the public slice before expanding editorial work. Test evidence will be saved under docs/verification. Production DNS, live mail provisioning and live content import remain outside this local build.
